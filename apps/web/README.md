# Web App

Next.js 前端应用骨架，负责上传、候选人管理、JD 管理和匹配分析页面。

## Local Env

前端本地开发使用 `apps/web/.env.local`。当前仓库已生成模板，启动前需要补齐：

- `NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY`

`NEXT_PUBLIC_API_BASE_URL` 默认指向 `http://localhost:3001`。
